# @ananta/types

Tipe TypeScript hasil generate dari skema OpenAPI backend, agar frontend & backend
selalu sinkron.

Jalankan saat API hidup di `localhost:8000`:

```bash
npm run gen:types
```

Outputnya `api.d.ts` di folder ini, lalu impor di frontend.
